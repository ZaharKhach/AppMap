import React from "react";
import { useSelector } from "react-redux";
import { selectCurrentUser } from "../../../slices/globalSlice";

const Button = () => {
 
  return (
    <div> button</div>
  );
};

export default Button;
